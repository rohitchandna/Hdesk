require "spec_helper"
require "cancan/matchers"

describe Ability do
  describe "as guest" do
    before(:each) do
      @ability = Ability.new(nil)
    end

    describe "ticket" do 
      before :each do 
        

    it "can only create a user" do

    end
  end
end
